<div class="search_bar">
<%= form_tag search_path, method: :get do %>
  <%= text_field_tag :search, params[:search], required: true %>
  <%= submit_tag t("products.search.search") %>
<% end %>
</div>
